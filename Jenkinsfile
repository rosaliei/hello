pipeline {
    agent any

    options {
        semanticVersioning()
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
